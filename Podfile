# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ReviewGrayGram' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReviewGrayGram

  # network library 
  pod 'Alamofire'
  # json mapper 
  pod 'ObjectMapper'
  # help layout 
  pod 'ManualLayout'
  # image library 
  pod 'Kingfisher'
  # auto layout 
  pod 'SnapKit'
end
